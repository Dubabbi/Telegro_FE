//Main.jsx
import * as S from './MainStyle';
import * as L from '../Login/LoginStyle';

export default function Main() {
    return (
        <L.AppContainer style={{marginTop: '30%'}}>
            df
        </L.AppContainer>
    );
}